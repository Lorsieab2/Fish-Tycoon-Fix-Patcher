# Fish Tycoon Fix Patcher v1.2.2

Fixes the crash that occurred after selecting OK on `Buy this Item?` while all
three supply slots were occupied. The confirmation dialog may overwrite ECX;
v1.2.2 reloads the verified game-state pointer before inspecting slot records.

Purchase confirmation, stacking, replacement selection, Unknown Chemical
behavior, base-game icon resources, and Explorer icon refresh remain enabled.
